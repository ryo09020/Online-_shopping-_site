<table  class="table table-hover table-borderless table-sm custom-table border border-secondary p-5">
    <tr><th>ジャンル</th></tr>
    <% Genre.all.each do |genre|%>
        <tr>
            <td>
                <%= link_to genre.name,items_path(genre_id: genre.id)%>
            </td>
        </tr>
    <% end %>
</table>